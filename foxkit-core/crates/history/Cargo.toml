[package]
name = "history"
version.workspace = true
edition.workspace = true
description = "Foxkit History - undo/redo and change history"

[dependencies]
foxkit-core = { path = "../foxkit-core" }

serde.workspace = true
parking_lot.workspace = true
