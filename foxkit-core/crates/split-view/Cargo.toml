[package]
name = "split-view"
version.workspace = true
edition.workspace = true
description = "Foxkit Split View - editor split panes and layout"

[dependencies]
buffer = { path = "../buffer" }

parking_lot.workspace = true
serde.workspace = true
tokio = { workspace = true, features = ["sync"] }

tracing = "0.1"
