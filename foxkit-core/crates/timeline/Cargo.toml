[package]
name = "timeline"
version.workspace = true
edition.workspace = true
description = "Foxkit Timeline - file history timeline view"

[dependencies]
git = { path = "../git" }

tokio.workspace = true
parking_lot.workspace = true
serde.workspace = true
chrono = { version = "0.4", features = ["serde"] }

anyhow = "1.0"
tracing = "0.1"
