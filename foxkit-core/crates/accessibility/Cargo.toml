[package]
name = "foxkit-accessibility"
version = "0.1.0"
edition = "2024"
description = "Accessibility and screen reader support for Foxkit"
license = "MIT OR Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
parking_lot = "0.12"
