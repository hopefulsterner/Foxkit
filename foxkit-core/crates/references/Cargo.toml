[package]
name = "references"
version.workspace = true
edition.workspace = true
description = "Foxkit References - find all references to symbols"

[dependencies]
lsp = { path = "../lsp" }
goto-definition = { path = "../goto-definition" }

tokio.workspace = true
parking_lot.workspace = true
serde.workspace = true

anyhow = "1.0"
tracing = "0.1"
async-trait.workspace = true
