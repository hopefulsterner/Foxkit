[package]
name = "gpu"
version.workspace = true
edition.workspace = true
description = "Foxkit GPU - hardware-accelerated rendering"

[dependencies]
tokio.workspace = true
parking_lot.workspace = true

anyhow = "1.0"
thiserror = "1.0"
tracing = "0.1"
raw-window-handle = "0.6"
wgpu.workspace = true
bytemuck = { version = "1.14", features = ["derive"] }
glam = "0.25"

# Font rasterization
fontdue = "0.8"
