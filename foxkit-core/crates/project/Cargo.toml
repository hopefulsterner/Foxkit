[package]
name = "project"
version.workspace = true
edition.workspace = true
description = "Foxkit Project - project detection and configuration"

[dependencies]
foxkit-core = { path = "../foxkit-core" }
fs = { path = "../fs" }

serde.workspace = true
serde_json = "1.0"
toml = "0.8"
anyhow = "1.0"
