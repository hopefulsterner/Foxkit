[package]
name = "problems"
version.workspace = true
edition.workspace = true
description = "Foxkit Problems Panel - diagnostics view"

[dependencies]
diagnostics = { path = "../diagnostics" }
lsp = { path = "../lsp" }

tokio.workspace = true
parking_lot.workspace = true
serde.workspace = true

anyhow = "1.0"
tracing = "0.1"
