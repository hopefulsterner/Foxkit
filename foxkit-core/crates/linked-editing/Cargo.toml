[package]
name = "linked-editing"
version.workspace = true
edition.workspace = true
description = "Foxkit Linked Editing - synchronized editing ranges"

[dependencies]
lsp = { path = "../lsp" }
buffer = { path = "../buffer" }

tokio.workspace = true
parking_lot.workspace = true
serde.workspace = true

anyhow = "1.0"
tracing = "0.1"
